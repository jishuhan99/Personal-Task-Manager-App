import { Stack } from 'expo-router';
import React from 'react';

export default function Layout() {
    return (
        // This Stack will manage the navigation between your screens.
        // It will render the current screen (e.g., index.tsx or detail.tsx)
        <Stack />
    );
}